apiVersion: v1
kind: Service
metadata:
  name: aurapredict-ai-service
  namespace: default
  labels:
    app: aurapredict-ai
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: aurapredict-ai
